@model List<Brand>


@{
    ViewData["Title"] = "Index";
}

<h2>Brands</h2>

<div class="list-group">
    @foreach (var brand in Model)
    {
        <a asp-controller="Model" asp-action="Index" asp-route-id="@brand.Id" class="list-group-item list-group-item-action">@brand.Name</a>
    }
</div>