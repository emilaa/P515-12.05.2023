@model List<Model>

@{
    ViewData["Title"] = "Index";
}

<h2>Models</h2>

<div class="list-group">
    @foreach (var modell in Model)
    {
            <a class="list-group-item list-group-item-action">@modell.Name</a>
    }
</div>
